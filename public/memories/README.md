# Memories Gallery Images

Place your memory photos here (e.g. `memory1.jpg`, `memory2.jpg`, etc.).
By default, the memories gallery uses emoji placeholders so the site remains beautiful even before you add photos.
If you want to use real photos, update the `src/components/MemoriesGallery.tsx` card images to reference your filenames!
